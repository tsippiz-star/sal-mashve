# 🌐 מדריך פריסה: מאתר מקומי לאתר ציבורי בעולם

מדריך צעד-אחר-צעד להעלאת האפליקציה לענן. **בלי להתקין שום דבר במחשב שלך.**
מכיל שני מסלולים לפי הצורך – התחילי במסלול A (הכי קל).

---

## 🥇 מסלול A: Streamlit Community Cloud (המומלץ להתחלה)

✅ **חינם לחלוטין ולתמיד**
✅ **בלי כרטיס אשראי, בלי הגבלת משתמשים**
✅ **הכי מהיר להקמה (10 דקות)**
✅ **קישור מקצועי כמו** `https://sal-mashve.streamlit.app`

### שלב 1: פתיחת חשבון GitHub (5 דקות)

GitHub הוא כמו Google Drive לקוד. Streamlit מושכת ממנו את הפרויקט אוטומטית.

1. גשי ל: **https://github.com**
2. **Sign up** – הרשמי עם Gmail (חינם)
3. אמתי את המייל שלך

### שלב 2: העלאת הפרויקט ל-GitHub (5 דקות)

1. אחרי הכניסה ל-GitHub, לחצי **+ למעלה משמאל → New repository**
2. **Repository name:** `sal-mashve` (או מה שתרצי)
3. **Visibility:** בחרי **Public** (חובה כדי ש-Streamlit יראה)
4. **⚠️ אל תסמני** את "Add a README"
5. **Create repository**

6. במסך הבא, לחצי על הקישור הכחול: **"uploading an existing file"**
7. חלצי את הקובץ `price-comparison-bot.zip`
8. **גררי את כל הקבצים** מתוך התיקייה (README.md, requirements.txt, .gitignore, ותיקיות src/ ו-static/) לחלון הדפדפן
9. חכי שההעלאה תסתיים, ולחצי כפתור ירוק **Commit changes**

### שלב 3: פריסה ב-Streamlit Cloud (3 דקות!)

1. גשי ל: **https://streamlit.io/cloud**
2. לחצי **Sign in with GitHub** → אשרי גישה
3. לחצי **New app** בפינה
4. מלאי:
   - **Repository:** `dana/sal-mashve` (מה שבחרת)
   - **Branch:** `main`
   - **Main file path:** `src/app.py`
5. **App URL:** תני שם מגניב, למשל `sal-mashve` → הכתובת תהיה `https://sal-mashve.streamlit.app`
6. לחצי **Deploy!**

⏱️ 3-5 דקות ואת תראי את האתר שלך חי. הקישור לשיתוף מיידי!

### שלב 4: הזרמת מחירים לאתר (חד-פעמי, 2 דקות)

בלוח הבקרה של Streamlit, לחצי על התפריט (⋮) ליד האפליקציה שלך → **Reboot app** לאחר שינויים.

יש שתי דרכים למלא מחירים:

**דרך 🅰 (הקלה):** צרי GitHub Action שיריץ את ה-scraper אוטומטית פעם ביום. הקובץ מצורף – ראי הסבר בסוף.

**דרך 🅱 (המהירה עכשיו):** הריצי את ה-scraper פעם אחת מהמחשב שלך (או ממחשב של חבר), והעלי את `prices.db` ל-GitHub.

---

## 🥈 מסלול B: Render.com (מקצועי יותר)

השתמשי בזה אם רוצה שליטה מלאה, שדרוגים בהמשך, או תוכנית תשלום עם ביצועים טובים יותר.

### שלב 1-2: זהים למסלול A (GitHub + העלאה)

### שלב 3: פריסה ב-Render

1. גשי ל: **https://render.com**
2. Sign up עם GitHub → אשרי
3. לחצי **+ New → Web Service**
4. בחרי את ה-repository שלך (`sal-mashve`)
5. הגדרות:
   - **Name:** `sal-mashve`
   - **Region:** Frankfurt (הכי קרוב לישראל)
   - **Branch:** main
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `streamlit run src/app.py --server.port $PORT --server.address 0.0.0.0`
   - **Instance Type:** Free
6. **Create Web Service**

⏱️ 5 דקות והאתר חי בכתובת: `https://sal-mashve.onrender.com`

**⚠️ בתוכנית החינמית של Render** – האתר "ישן" אחרי 15 דקות בלי גישה, ולוקח 30 שניות לעורר. פתרון: שדרוג ל-$7/חודש כשיש קהל, או להישאר ב-Streamlit Cloud.

---

## 📱 הפיכת האתר ל-PWA (התקנה כאפליקציה על הנייד)

**בונוס ענק:** בזכות קובץ `manifest.json` שכבר קיים בפרויקט, כל אחד שנכנס לאתר יכול:

**באנדרואיד (Chrome):**
1. פותח את הקישור
2. מקבל אוטומטית כפתור בתחתית: "Add to Home Screen"
3. לוחץ ומקבל אייקון אמיתי על מסך הבית

**באייפון (Safari):**
1. פותח את הקישור
2. לוחץ על ⬆️ (שיתוף)
3. גולל למטה → "Add to Home Screen"
4. אייקון מופיע בבית!

**המשתמשים חושבים שזו אפליקציה אמיתית** – בלי חנות אפליקציות, בלי התקנה מסובכת.

### אייקונים לאפליקציה

בפרויקט כרגע יש רק `manifest.json` בלי אייקונים. **תיצרי אייקונים בקלות ב-30 שניות:**

1. גשי ל: **https://favicon.io/emoji-favicons/shopping-cart**
2. הורידי את החבילה
3. שני שם ל-`icon-192.png` ול-`icon-512.png`
4. העלי לתיקייה `static/` ב-GitHub

וזהו – יש לך אפליקציה עם אייקון עגלת קניות!

---

## 🔄 עדכון מחירים אוטומטי (GitHub Actions)

כדי שהמחירים יתעדכנו אוטומטית פעם ביום, יש קובץ מוכן בתיקייה `.github/workflows/scrape.yml`.

**איך זה עובד:**
- GitHub מריץ בכל בוקר את `scraper.py`
- ה-DB המעודכן מועלה אוטומטית ל-repository
- Streamlit Cloud טוען את הגרסה החדשה תוך דקות

**להפעלה:**
1. ודאי שהתיקייה `.github/workflows/scrape.yml` הועלתה ל-GitHub
2. GitHub → **Actions** → אשרי הפעלת workflows
3. זהו! מעכשיו העדכון אוטומטי כל בוקר בשעה 06:00 ישראל

**⚠️ מגבלה:** GitHub Actions רץ מ-IP אמריקאי, ולכן רק שופרסל נגישה משם.
לרשתות אחרות (רמי לוי, יוחננוף וכו') תזדקקי לפתרון שרץ מ-IP ישראלי:
- מחשב פרטי בישראל שרץ פעם ביום Cron
- Raspberry Pi ביתי (₪250 חד-פעמי)
- שירות VPN ישראלי בענן (בסביבות $5 בחודש)

---

## 💰 מפת ההוצאות לפי גודל

| שלב | משתמשים | פלטפורמה | עלות חודשית | פעולות |
|---|---|---|---|---|
| **התחלה** | 0-100 | Streamlit Cloud | **חינם** | פריסה בסיסית |
| **גדילה** | 100-1,000 | Streamlit Cloud + GitHub Actions | **חינם** | עדכון מחירים אוטומטי |
| **קהל** | 1,000-10,000 | Render Starter | **$7** | ביצועים משופרים, לא ישן |
| **גדול** | 10K-100K | Render Pro + Supabase | **~$25** | מסד נתונים בענן, גיבויים |
| **מסחרי** | 100K+ | AWS/Azure | **תלוי** | Load balancer, CDN |

**הסוד:** מתחילות בחינם. משדרגות רק כשיש הכנסה או קהל שדורש את זה.

---

## 🎁 בונוס: איך משתפים את האתר?

1. **קישור ישיר** – שלחי בוואטסאפ / SMS / מייל
2. **QR Code** – צרי ב-https://qr-code-generator.com והדפיסי מדבקות
3. **מודעה בפייסבוק/אינסטגרם** – פוסט אחד → הרבה משתמשים
4. **קבוצות רלוונטיות** – "המחזור שלי", "חוסכות עם רותי", אמהות ישראליות
5. **תג בהפצה:** בשלב מאוחר יותר – שיווק דיגיטלי ממומן

---

## ❓ שאלות נפוצות

**שאלה: האם המשתמשים יראו את הקוד שלי?**
תשובה: אם ה-repo הוא Public – כן, את הקוד. הם לא רואים את הנתונים / הסודות (אם אלה ב-Environment Variables).

**שאלה: האם אני צריכה כרטיס אשראי?**
תשובה: לא לחלוטין למסלול A. Render Free גם לא מבקש. תשלום רק בשדרוגים.

**שאלה: האם האתר יעבוד גם על אייפון?**
תשובה: כן, בכל דפדפן מודרני. ב-Safari, Chrome, Edge, Firefox.

**שאלה: מה קורה אם הרבה אנשים משתמשים בו-זמנית?**
תשובה: Streamlit Cloud תומך ב-40 משתמשים בו-זמנית בחבילה החינמית. מעל זה – Render Free עם 750 שעות/חודש, ובהמשך שדרוג.

**שאלה: איך אני יודעת כמה משתמשים יש?**
תשובה: **Google Analytics** – 5 דקות להתקין. בכל דף Streamlit יש הזרקה של קוד GA. אני יכול לעזור לך להוסיף.

---

## 🚀 הצעד הבא – מה תרצי?

יש לך 3 אפשרויות מומלצות מכאן:

1. **התחלה קלה** – פשוט לפרוס עכשיו במסלול A (Streamlit Cloud) ולראות את זה חי
2. **Google Analytics** – להוסיף מעקב משתמשים לראות כמה נכנסים לאתר
3. **דומיין משלך** – לקנות `סל-משווה.co.il` (~₪60 בשנה) ולחבר לאתר

**אחרי שהאתר חי – נחשוב יחד על ההפצה.** 💪
