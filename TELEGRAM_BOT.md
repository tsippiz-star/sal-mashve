# 📱 בוט הטלגרם – מדריך הפעלה מלא

בוט טלגרם דינמי, בעברית, לניהול סל קניות והשוואת מחירים בין 6 רשתות המזון בישראל.
עובד בדיוק כמו וואטסאפ – את מדברת איתו רגיל, בטבעיות.

---

## 🎬 מה הבוט עושה

- ✅ **מקבל רשימת קניות** בעברית, בטקסט חופשי (שורה לפריט, או מופרד בפסיקים)
- ✅ **זוכר את הסל שלך** גם אחרי סגירת האפליקציה (SQLite)
- ✅ **משווה בזמן אמת** בין הרשתות עם 🏆 לזול ביותר בכל פריט
- ✅ **כפתורים אינטראקטיביים** – השוו / הוסיפי / נקי (בלי לזכור פקודות)
- ✅ **היסטוריה + חיסכון מצטבר** – יודעת בדיוק כמה חסכת החודש
- ✅ **תפריט Reply Keyboard** קבוע בתחתית עם קיצורי דרך
- ✅ **פקודת /refresh** למנהלים – מעדכן נתוני מחירים בלחיצה

---

## 🚀 הקמה מלאה – 10 דקות

### שלב 1: יצירת בוט חדש בטלגרם

1. בטלגרם חפשי: `@BotFather`
2. שלחי `/newbot`
3. תני שם: `בוט המחירים שלי` (או מה שתרצי)
4. תני username: `my_prices_2026_bot` (חייב להסתיים ב-bot)
5. **BotFather ישלח לך TOKEN** – ייראה כמו:
   `123456789:AAG-abcdefghijklmnopQRSTUVwxyz1234567`
6. שמרי אותו (זה כמו סיסמה של הבוט)

### שלב 2: מציאת ה-user-id שלך (למנהל)

1. שלחי הודעה ל-`@userinfobot` בטלגרם
2. הוא ישיב לך עם ה-id שלך (מספר, למשל `123456789`)

### שלב 3: התקנה על המחשב

```bash
cd price-comparison-bot
python3 -m venv venv
source venv/bin/activate           # Windows: venv\Scripts\activate
pip install -r requirements.txt

# מוריד נתונים ראשוני (~2 דק')
python3 src/scraper.py --chains shufersal --limit 5
```

### שלב 4: הפעלת הבוט

```bash
export TELEGRAM_BOT_TOKEN="123456789:AAG-abcdefg..."
export TELEGRAM_ADMIN_IDS="123456789"          # ה-user-id שלך
python3 src/telegram_bot.py
```

**Windows PowerShell:**
```powershell
$env:TELEGRAM_BOT_TOKEN="123456789:AAG-abcdefg..."
$env:TELEGRAM_ADMIN_IDS="123456789"
python src/telegram_bot.py
```

תראי בטרמינל: `Bot starting...` – זהו, הבוט חי.

### שלב 5: בואי נתנסה!

1. פתחי טלגרם בנייד/מחשב
2. חפשי את הבוט לפי ה-username שיצרת (`@my_prices_2026_bot`)
3. שלחי `/start`
4. שלחי:
   ```
   חלב 3%
   קוטג' 5%
   ביצים
   במבה
   ```
5. לחצי על 🔍 השווי סל – ותקבלי תוצאה עם ההפרשים!

---

## 🎨 חוויית המשתמש

### תפריט קבוע בתחתית (Reply Keyboard):
```
┌─────────────────┬─────────────────┐
│   🔍 השווי סל   │   📋 הסל שלי    │
├─────────────────┼─────────────────┤
│   📊 היסטוריה   │   🗑️ נקי סל    │
└─────────────────┴─────────────────┘
```

### דוגמת פלט:
```
📊 השוואת סל • 5 פריטים

🛒 חלב 3%
   חלב תנובה 3% קרטון 1 ליטר
      🏆 רמי לוי: 5.90 ₪
         שופרסל: 6.90 ₪ (+17%)
         יוחננוף: 6.50 ₪ (+10%)

🛒 קוטג' 5%
   קוטג' תנובה 5% 250 גרם
      🏆 יוחננוף: 5.90 ₪
         רמי לוי: 6.20 ₪ (+5%)
   ...

━━━━━━━━━━━━━━━━━━━
💰 סה"כ סל לרשת

🏆 רמי לוי: 87.40 ₪ (5/5)
   יוחננוף: 89.10 ₪ (5/5)
   שופרסל: 94.30 ₪ (5/5)

💸 חיסכון של 6.90 ₪ ברמי לוי
```

---

## ☁️ פריסה חינמית 24/7 (הבוט חי גם כשהמחשב שלך כבוי)

### אופציה A: Render.com (מומלץ, חינם לחלוטין)

1. פתחי חשבון בחינם ב-[render.com](https://render.com)
2. צרי GitHub repo עם הפרויקט (GitHub Desktop קל מאוד)
3. ב-Render: New → Background Worker
4. חברי את ה-repo
5. הגדרות:
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python src/telegram_bot.py`
   - **Environment Variables:**
     - `TELEGRAM_BOT_TOKEN` = הטוקן שלך
     - `TELEGRAM_ADMIN_IDS` = ה-user-id
6. Deploy → הבוט חי!

**הערה:** נדרש לרוץ scraper.py **מקומית** מישראל אחת ליום ולעדכן את ה-DB
(או להקים Cron מ-IP ישראלי).

### אופציה B: Railway.app

זהה ל-Render, קצת יותר נוח לבסיסי נתונים.

### אופציה C: Raspberry Pi בבית שלך (₪250 חד-פעמי, IP ישראלי!)

הכי מומלץ למי שרוצה גם לרוץ את ה-scraper בישראל.
- Raspberry Pi 4 (~₪250)
- SD Card 32GB
- מריצה `python3 src/telegram_bot.py` + Cron יומי ל-scraper
- **צריכת חשמל: ~3W** = 15 ₪ בשנה

```bash
# תוכן קובץ crontab (crontab -e)
0 6 * * * cd /home/pi/price-comparison-bot && ./venv/bin/python src/scraper.py --all --limit 3
```

---

## 🔧 בעיות שכיחות ופתרונות

| בעיה | פתרון |
|---|---|
| `ModuleNotFoundError: telegram` | `pip install python-telegram-bot` |
| `Unauthorized` | הטוקן שגוי או שלא הועבר. בדקי `echo $TELEGRAM_BOT_TOKEN` |
| הבוט לא עונה | ודאי שהתהליך רץ בטרמינל, ושהבוט לא חסום ב-@BotFather |
| "אין נתונים ב-DB" | תריצי קודם: `python3 src/scraper.py --chains shufersal` |
| פריט לא זוהה | כתבי שם מפורש יותר: "חלב תנובה 3% קרטון" |
| הבוט מגיב לאט | ה-fuzzy matching חוסם – בגרסה עתידית עם Redis Cache |

---

## 🔐 אבטחה חשובה!

- ❌ **אל תעלי את הטוקן ל-GitHub** – הוסיפי `.env` ל-.gitignore
- ✅ אם הטוקן דלף – `/revoke` ב-@BotFather ואז `/token` לחדש
- ✅ **מגבלת גישה** – את יכולה להוסיף לקוד בדיקה שרק user-id מאושרים ידברו עם הבוט
- ✅ **הגבלת קצב** – טלגרם מגביל אוטומטית ל-30 הודעות בשנייה, לא צריך לחשוב על זה

### הגבלה למשתמשים מוגדרים (אם רוצה בוט פרטי לחלוטין):

הוסיפי לתחילת `handle_text`:

```python
ALLOWED_USERS = [123456789, 987654321]  # user-id שלך + משפחה
if update.effective_user.id not in ALLOWED_USERS:
    await update.message.reply_text("⛔ הבוט פרטי.")
    return
```

---

## 🎁 שדרוגים מוצעים לעתיד

- [ ] **תזכורות אוטומטיות** – "שלום, מוצאי שבת, זמן לסל!"
- [ ] **שיתוף רשימה בין בני משפחה** – רשימה משותפת עם שני user-ids
- [ ] **צילום קבלה** – OCR עם GPT-4o Vision שמזין את המוצרים אוטומטית
- [ ] **תזכיר לי כשמוצר בהנחה** – מעקב אחר מבצעים על מוצרים ברשימה
- [ ] **מיקום ה-GPS** – "מצא את הסניף הזול הקרוב אליך"
- [ ] **גרפים** – שליחת חיסכון חודשי כתמונה עם matplotlib

---

## 📞 סימולציית שיחה אמיתית

```
את:    /start
בוט:   👋 ברוכה הבאה! שלחי לי רשימת קניות...

את:    חלב 3%, קוטג', ביצים, במבה, לחם
בוט:   ✅ נוספו 5 פריטים. סה"כ בסל: 5
       [🔍 השווי סל עכשיו] [➕ עוד] [🗑️ נקי]

את:    (לוחצת) 🔍 השווי סל עכשיו
בוט:   🔎 מחפש 5 פריטים...
       📊 השוואת סל • 5 פריטים
       ...
       🏆 רמי לוי: 34.20 ₪
       💸 חיסכון של 4.70 ₪

יומיים אחרי:
את:    /history
בוט:   📊 10 סלים אחרונים:
       • 17.9 – רמי לוי: 34.20 ₪ (חסכת 4.70)
       • 15.9 – יוחננוף: 87.90 ₪ (חסכת 12.30)
       💰 חיסכון מצטבר: 17.00 ₪ ב-2 סלים
```

---

Built with 🐍 python-telegram-bot 22.8 + SQLite + RapidFuzz.
